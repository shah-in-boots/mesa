# Restructure models to fit within a model table Passes information to `new_model_table()` for initialization

Restructure models to fit within a model table Passes information to
`new_model_table()` for initialization

## Usage

``` r
construct_table_from_models(x, ...)
```

## Arguments

- x:

  Single-element, possibly-named list holding a `mdl` vector
