# Pick the dataset a model table's rows refer to

The attached data is the canonical source for a categorical term's
levels. When several datasets are attached, the one the rows reference
is used; a single attached dataset is taken as-is.

## Usage

``` r
selection_data(x)
```
