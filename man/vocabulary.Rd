% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patterns.R, R/vocabulary.R
\name{formula_patterns}
\alias{formula_patterns}
\alias{vocabulary}
\alias{term_roles}
\alias{term_transformations}
\title{The \code{mesa} vocabulary}
\usage{
formula_patterns()

term_roles()

term_transformations()
}
\value{
A named list (\code{term_roles()}) or character vector (others)
}
\description{
The grammar of \code{{mesa}} is built on small, controlled vocabularies: the
causal \emph{roles} a term can play, the \emph{patterns} by which formulas expand,
and the \emph{transformations} a term can carry. These accessors expose the
vocabularies so they can be inspected (and so error messages can point
somewhere authoritative).
\itemize{
\item \code{term_roles()} returns the named list of roles and their formula
shortcuts (e.g. \code{exposure = ".x"})
\item \code{term_transformations()} returns the recognized transformation wrappers
(e.g. \code{log()}); a wrapped term keeps its full call as its name and
records the wrapper in its \code{transformation} field
\item \code{formula_patterns()} returns the names of the registered expansion
patterns available to \code{\link[=fmls]{fmls()}} (see \code{\link[=register_pattern]{register_pattern()}} to add one)
}
}
