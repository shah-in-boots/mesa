% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{identify_families}
\alias{identify_families}
\title{Sort a \code{fmls} object's formulas into families}
\usage{
identify_families(x, data = NULL)
}
\arguments{
\item{x}{A \code{fmls} object}

\item{data}{An optional \code{data.frame}; when supplied, terms are stamped
with \code{\link[=set_data]{set_data()}} so stratifying terms report their observed levels}
}
\value{
A \code{tbl_df} with one row per formula, in the object's order: \code{family}
(integer id, by order of first appearance), \code{pattern}, \code{relation} (\code{NA}
for a family that stands alone), \code{formula_call}, \code{outcome}, \code{exposure},
\code{mediator}, \code{strata}, and \code{covariates} (a list of the adjustment terms).
}
\description{
A family is a set of formulas that belong to one analysis: an adjustment
ladder climbing toward a fully adjusted model, a mediation triad, parallel
adjustment sets around one exposure. Reads the causal roles a \code{fmls} object
carries and sorts its formulas into families, naming each family's
\emph{pattern} and — where families relate to one another — the \emph{relation}
between them.
}
\details{
Formulas group into a family by their outcome and exposure; a mediator
binds its triad (per \code{\link[=fmls]{fmls()}}'s mediation expansion) into a single family
across those boundaries. Within a family the adjustment sets decide the
pattern: \code{mediation} (the formulas share a mediator-role term), \code{sequential}
(the adjustment sets nest, each containing the last), \code{parallel} (adjustment
sets that do not nest), or \code{direct} (a single formula). Families that share
an outcome and adjustment ladder over different exposures relate as \verb{varied exposures} (the wide-table shape); over different outcomes, \verb{varied outcomes}.

Stratifying terms (\code{.s()}) do not split a family — stratification expands at
\code{\link[=fit]{fit()}} time — so the stratum rides along in the \code{strata} column. Supplying
\code{data} stamps the terms first (see \code{\link[=set_data]{set_data()}}) so the strata column reports
its observed levels, e.g. \verb{am (2 levels)}.
}
