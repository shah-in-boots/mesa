% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-table-helpers.R
\name{keep_models}
\alias{keep_models}
\title{Keep the models that share a causal context}
\usage{
keep_models(
  x,
  outcome = NULL,
  exposure = NULL,
  mediator = NULL,
  interaction = NULL,
  strata = NULL,
  level = NULL,
  subset = NULL,
  family = NULL,
  pattern = NULL,
  relation = NULL
)
}
\arguments{
\item{x}{A \code{mdl_tbl} object}

\item{outcome, exposure, mediator, interaction}{Keep models carrying this
term in this causal role (bare names or strings; several combine as OR)}

\item{strata, level, subset}{Keep models fit under this stratifying term,
stratum level, or subset rule}

\item{family}{Keep these family id(s), from the columns
\code{\link[=identify_family]{identify_family()}} stamped on the table}

\item{pattern}{Keep families with this pattern: \code{"sequential"},
\code{"parallel"}, \code{"mediation"}, or \code{"direct"}}

\item{relation}{Keep families carrying this relation: \code{"varied exposures"} or \code{"varied outcomes"}}
}
\value{
The whittled \code{mdl_tbl}, its attributes pruned to the remaining
models; a message reports how many models were kept.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{keep_models()} whittles a \code{mdl_tbl} down to the models that belong
together — the step before \code{\link[=mdl_gt]{mdl_gt()}}, whose gate admits only one
presentable analysis. Models are kept by the causal roles they carry
(\code{outcome}, \code{exposure}, \code{mediator}, \code{interaction}), by their fitting
context (\code{strata}, \code{level}, \code{subset}), or by the family structure
\code{\link[=identify_family]{identify_family()}} reads (\code{family}, \code{pattern}, \code{relation}). Conditions
combine with AND; several values in one argument combine with OR.

Ordinary \code{dplyr} verbs still work on the table — \code{keep_models()} is the
causal-aware shorthand. What it adds over \code{dplyr::filter()}:
\itemize{
\item arguments take \strong{bare names or strings} (\code{keep_models(x, outcome = mpg)}, \code{keep_models(x, outcome = c(mpg, qsec))}), matched by exact
identity, never by substring;
\item every requested value is \strong{checked against what the table holds}, so a
typo errors with the available values instead of silently keeping
nothing;
\item \code{pattern} and \code{relation} are identified on the spot when the table has
not been stamped; a \code{relation} matches a family carrying it among
several (\code{"varied exposures, varied outcomes"});
\item \code{family} ids are positional, so they must come from a stamp the user
has \emph{seen}: \code{x |> identify_family() |> keep_models(family = 1)} (look
first, then cut).
}

Stamped \code{family}/\code{pattern}/\code{relation} columns describe the identification
at stamp time; after whittling, re-run \code{\link[=identify_family]{identify_family()}} to refresh
them (ids renumber from 1).
}
\examples{
d <- mtcars
mt <-
  c(
    fmls(mpg ~ .x(wt) + hp + cyl, pattern = "sequential"),
    fmls(mpg ~ .x(disp) + hp + cyl, pattern = "sequential"),
    fmls(qsec ~ .x(am))
  ) |>
  fit(.fn = lm, data = d) |>
  model_table(data = d)

# By causal role, bare names or strings
keep_models(mt, outcome = mpg)
keep_models(mt, exposure = c(wt, disp))

# By the identified family structure
keep_models(mt, relation = "varied exposures")
mt |> identify_family() |> keep_models(family = 1)

}
\seealso{
\code{\link[=identify_family]{identify_family()}} to see the family structure first,
\code{\link[=adjustment_sets]{adjustment_sets()}} to see the adjustment rungs, \code{\link[=mdl_gt]{mdl_gt()}} for the
gate this prepares for
}
