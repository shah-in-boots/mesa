% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-columns.R
\name{add_events}
\alias{add_events}
\alias{add_rate_difference}
\title{Add data-statistic columns to a \verb{<mesa>}}
\usage{
add_events(x, followup, person_years = 100, scale = 365.25, digits = 1)

add_rate_difference(x, conf_level = 0.95)
}
\arguments{
\item{x}{A \verb{<mesa>} specification (from \code{\link[=mesa]{mesa()}})}

\item{followup}{The column of the attached data holding each subject's
follow-up time, as a bare name or a string. When the mesa's outcome is a
\code{Surv()} call, \code{followup} is inferred from its time argument and may be
omitted; a plain outcome (or outcomes that disagree on the time column)
still requires it explicitly. Supplying \code{followup} always overrides the
inference, e.g. when the attached data's follow-up column is not the one
named in the fitted formula}

\item{person_years}{The person-time denominator the rates are expressed in
(\code{100} shows rates per 100 person-years)}

\item{scale}{The divisor turning the \code{followup} units into years, passed to
\code{\link[survival:pyears]{survival::pyears()}}: the default \code{365.25} reads follow-up recorded in
days; use \code{1} when follow-up is already in years}

\item{digits}{Number of digits the rates (and the rate difference) are
formatted to}

\item{conf_level}{Confidence level of the rate-difference interval}
}
\value{
The modified \verb{<mesa>} specification.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

These verbs append \emph{column blocks} whose statistics come from the models'
\strong{attached data} (see \code{\link[=attach_data]{attach_data()}}), not from the fitted coefficients —
the M6.1 statistics vocabulary's load-bearing distinction. Like every
\verb{<mesa>} verb they only record instructions; the statistics are computed
when the table is realized by \code{\link[=as_gt]{as_gt()}}, against the dataset each model's
\code{data_id} resolves to.
\itemize{
\item \code{add_events()} adds the event count and the incidence rate per
\code{person_years} for every displayed term level, computed by
\code{\link[survival:pyears]{survival::pyears()}} from the follow-up time column and the outcome's
event indicator.
\item \code{add_rate_difference()} adds the incidence-rate difference between a
dichotomous term's two levels (non-reference minus reference), with a
normal-approximation confidence interval. It is a \emph{term-scoped} statistic
— computed across the levels, displayed in a column of its own — and it
reads the follow-up, person-years, and scale recorded by \code{add_events()},
so the specification must carry both blocks.
}
}
\details{
\subsection{The rate computations}{

Person-time per level comes from \code{survival::pyears(Surv(followup, event) ~ term, scale = scale)}; the event indicator is the outcome itself when it is
a column of the data, or the event argument of a \code{Surv()} outcome. The
incidence rate is \code{events / (person-time / person_years)}. The rate
difference between levels \code{2} and \code{1} has standard error \code{sqrt(events_2 / persontime_2^2 + events_1 / persontime_1^2)} (the Poisson variance of each
rate), and the interval uses the \code{qnorm(1 - (1 - conf_level) / 2)} critical
value — for the default \code{conf_level = 0.95}, \code{qnorm(0.975)}.

\code{add_rate_difference()} errors on any displayed term whose attached-data
factor does not have exactly two levels.
}
}
\seealso{
\code{\link[=mesa]{mesa()}}, \code{\link[=as_gt]{as_gt()}}, \code{\link[=attach_data]{attach_data()}}
}
