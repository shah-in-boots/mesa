% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-selection.R
\name{adjustment_set_index}
\alias{adjustment_set_index}
\title{The adjustment-set index of every model row}
\usage{
adjustment_set_index(x)
}
\value{
An integer vector, one entry per row of \code{x}.
}
\description{
Each distinct adjustment set — the covariates a model carries beyond its
outcome, exposure, mediator, and meta terms — is one rung, numbered by set
size and then order of first appearance. The index is the \emph{identity} of
the set, not its position within a family: models carrying the same
covariates share a rung wherever they sit (so related families' rows align
on the mesa by the actual adjustment), and different sets never collide,
even at equal term counts. \code{\link[=adjustment_sets]{adjustment_sets()}} displays the mapping.
}
\keyword{internal}
