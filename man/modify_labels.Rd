% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-spec.R
\name{modify_labels}
\alias{modify_labels}
\title{Relabel terms, levels, or columns late}
\usage{
modify_labels(x, ..., columns = NULL)
}
\arguments{
\item{x}{A \verb{<mdl_gt>} specification}

\item{...}{Labeled formulas relabeling terms or levels (see Description)}

\item{columns}{A labeled-formula input relabeling statistic columns}
}
\value{
The modified \verb{<mdl_gt>} specification.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{modify_labels()} rethinks a label without reselecting anything. It absorbs
the old \code{level_labels} argument of the retired \verb{tbl_*} functions, so its
labeled formulas may name either a term (the variable) or a specific level:
\itemize{
\item a variable with a scalar label relabels the \strong{term}
(\code{smoking ~ "Smoking status"});
\item a variable with a vector label relabels the term's \strong{levels} in
ascending order (\code{am ~ c("Manual", "Automatic")});
\item a bare level value relabels that \strong{level} wherever it appears
(\code{0 ~ "Absent"}).
}

Column (statistic) relabelings are supplied through \code{columns} and consumed
by the column verbs. Like every verb, \code{modify_labels()} merges: naming a
term, level, or column again replaces just that one label, with a message
naming it, while every other label already recorded — from this call or an
earlier one — stands (the \code{{ggplot2}} \code{labs()} merge behavior). So
rethinking one label late never forces restating the rest.
}
\seealso{
\code{\link[=mdl_gt]{mdl_gt()}}
}
