% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-columns.R
\name{add_interaction}
\alias{add_interaction}
\title{Add interaction rows to a \verb{<mesa>}}
\usage{
add_interaction(x, conf_level = 0.95)
}
\arguments{
\item{x}{A \verb{<mesa>} specification (from \code{\link[=mesa]{mesa()}})}

\item{conf_level}{Confidence level of the within-level intervals}
}
\value{
The modified \verb{<mesa>} specification.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{add_interaction()} puts effect modification on the mesa: one row per level
of each model's interaction variable, grouped by interaction term, with the
exposure's within-level estimate derived from the stored coefficients and
variance-covariance matrix by \code{\link[=estimate_interaction]{estimate_interaction()}} — nothing is refit.

The block \emph{defines} the rows of the table, so declaring it \strong{implies} the
\code{"interaction"} layout: if no layout has been declared yet, \code{add_interaction()}
selects it (with a message); if \code{\link[=modify_layout]{modify_layout()}} already declared a
different preset, \code{add_interaction()} errors naming the conflict rather than
silently overriding it. \code{modify_layout(preset = "interaction")} on its own
— without \code{add_interaction()} — still errors at realization, since the
block is what defines the rows. Its statistics carry two scopes: the
per-level cells (estimate/CI from \code{\link[=add_estimates]{add_estimates()}}; the per-level \code{n} from
\code{\link[=add_n]{add_n()}}, which counts the attached data) are ordinary rows, while the
single across-levels interaction p-value is a \emph{group-scoped} cell the
renderer floats over the level rows. A forest column (\code{\link[=add_forest]{add_forest()}}) reads
the same per-level estimates.
}
\seealso{
\code{\link[=mesa]{mesa()}}, \code{\link[=modify_layout]{modify_layout()}}, \code{\link[=estimate_interaction]{estimate_interaction()}}
}
