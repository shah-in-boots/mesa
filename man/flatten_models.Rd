% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-table-helpers.R
\name{flatten_models}
\alias{flatten_models}
\title{Flatten a model table to its parameter estimates}
\usage{
flatten_models(x, exponentiate = NULL, which = NULL, ...)
}
\arguments{
\item{x}{A \code{mdl_tbl} object}

\item{exponentiate}{Controls exponentiation of estimates and confidence
intervals. The default \code{NULL} infers per model from its family and link;
\code{TRUE}/\code{FALSE} forces the decision for all models.}

\item{which}{A \code{character} vector of model names (the \code{name} column) to
exponentiate, overriding the family-based inference for everything else.}

\item{...}{Arguments to be passed to or from other methods}
}
\value{
A \code{tibble} with one row per model parameter, carrying the model
context columns (\code{formula_call}, \code{outcome}, \code{strata}, ...), model-level
statistics from \code{\link[broom:glance]{broom::glance()}}, the parameter estimates from
\code{\link[broom:tidy]{broom::tidy()}}, and an \code{exponentiated} marker.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A \code{mdl_tbl} object can be flattened to its specific parameters, their
estimates, and model-level summary statistics -- one row per model term.
Models that were not fit (or failed) are dropped with a message; see
\code{\link[=model_failures]{model_failures()}} for why they failed. This relies on the \code{\link[broom:tidy]{broom::tidy()}}
output stored when the models were made.
}
\section{Exponentiation}{
By default (\code{exponentiate = NULL}), estimates and confidence intervals are
exponentiated when the model family calls for it -- Cox models and
generalized linear models on a log, logit, or complementary log-log link
(giving hazard, odds, or rate ratios) -- and left on the linear scale
otherwise (e.g. \code{lm}). A message reports when this happens, and the
\code{exponentiated} column records the decision per row. Set \code{exponentiate}
to \code{TRUE} or \code{FALSE} to override the inference for every model, or use
\code{which} to exponentiate only the models named there.
}

