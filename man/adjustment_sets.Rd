% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-table-helpers.R
\name{adjustment_sets}
\alias{adjustment_sets}
\alias{adjustment_sets.mdl_tbl}
\alias{adjustment_sets.mdl_gt}
\title{The adjustment sets of a model table}
\usage{
adjustment_sets(x, ...)

\method{adjustment_sets}{mdl_tbl}(x, ...)

\method{adjustment_sets}{mdl_gt}(x, ...)
}
\arguments{
\item{x}{A \code{mdl_tbl} object, or a \verb{<mdl_gt>} specification (read from its
fitted models)}

\item{...}{Arguments to be passed to or from other methods}
}
\value{
A \code{tibble} with one row per adjustment set: \code{adjustment} (the
index \code{select_adjustment()} uses), \code{covariates} (the set, \code{+}-joined;
\code{(unadjusted)} when empty), \code{adds} (what the rung adds over the
previous one, when it nests), \code{models} (how many models carry it), and
\code{families} (which families, by the stamped ids when present).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{adjustment_sets()} shows the adjustment rungs a table's models climb:
one row per distinct covariate set, numbered the way \code{\link[=mdl_gt]{mdl_gt()}}'s
\code{select_adjustment()} selects them. The index is the \emph{identity} of the
set — models carrying the same covariates share a rung wherever they sit,
so related families' rows align on the mesa by the actual adjustment —
and rungs are ordered by set size and then order of first appearance,
which for a \code{"sequential"} \code{\link[=fmls]{fmls()}} pattern is the order the ladder was
built.
}
\examples{
d <- mtcars
mt <-
  fmls(mpg ~ .x(wt) + hp + cyl, pattern = "sequential") |>
  fit(.fn = lm, data = d) |>
  model_table(data = d)
adjustment_sets(mt)

}
\seealso{
The \link{paring} verbs (especially \code{\link[=adjusting_for]{adjusting_for()}}),
\code{\link[=identify_family]{identify_family()}}, \code{\link[=mdl_gt]{mdl_gt()}}
}
