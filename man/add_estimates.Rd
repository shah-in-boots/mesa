% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-columns.R
\name{add_estimates}
\alias{add_estimates}
\alias{add_n}
\title{Add model-statistic columns to a \verb{<mesa>}}
\usage{
add_estimates(
  x,
  columns = list(beta ~ "Estimate", conf ~ "95\% CI", p ~ "P value"),
  exponentiate = NULL,
  digits = NULL
)

add_n(x, label = "N")
}
\arguments{
\item{x}{A \verb{<mesa>} specification (from \code{\link[=mesa]{mesa()}})}

\item{columns}{Which estimate statistics to show, as labeled formulas (see
\code{\link[=labeled_formulas_to_named_list]{labeled_formulas_to_named_list()}}). The recognized statistics are \code{beta}
(the point estimate), \code{conf} (the confidence interval), and \code{p} (the
p-value); the labels become the column headers}

\item{exponentiate}{Controls the scale of the estimates: \code{NULL} (default)
infers per model family (see Details), \code{TRUE}/\code{FALSE} overrides}

\item{digits}{Number of digits the estimate and interval are formatted to;
unset, the table-wide default applies (see \code{\link[=modify_style]{modify_style()}})}

\item{label}{The column header for the \code{n} column}
}
\value{
The modified \verb{<mesa>} specification.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

These verbs append a \emph{column block} to a \code{\link[=mesa]{mesa()}} specification — an
instruction for a set of statistic columns, computed and formatted only when
the table is realized by \code{\link[=as_gt]{as_gt()}}.
\itemize{
\item \code{add_estimates()} declares the estimate block: which of the point estimate
(\code{beta}), confidence interval (\code{conf}), and p-value (\code{p}) columns are
shown, under what labels, on what scale, and to how many digits. The
estimate and interval render merged into a single displayed column
(\code{1.4 (1.1, 1.8)}); the p-value renders as its own column.
\item \code{add_n()} adds the model-level number of observations, read from the
\code{nobs} recorded at fit time — so it never needs attached data.
}

A bare specification without \code{add_estimates()} already renders estimate +
CI; call the verb when you want to choose the statistics, their labels
(\code{beta ~ "HR"}), the scale, or the digits. Re-calling a verb replaces its
earlier block with a message (the same replacement behavior as every other
verb).
}
\details{
\subsection{Exponentiation}{

\code{exponentiate = NULL} (the default) defers the scale decision to the model
family inference of \code{\link[=flatten_models]{flatten_models()}}: Cox-family models and GLMs on a
\code{log}/\code{logit}/\code{cloglog} link come back exponentiated (hazard, odds, or rate
ratios), everything else stays linear. \code{TRUE} or \code{FALSE} overrides the
inference for every model on the mesa.
}

\subsection{Formatting}{

\code{digits} applies to the estimate and its interval; left unset, it falls to
the table-wide default (\code{\link[=modify_style]{modify_style()}}'s \code{digits}, or 2). P-values render
with three decimals, with values below shown as \verb{<0.001}.
}
}
\seealso{
\code{\link[=mesa]{mesa()}}, \code{\link[=as_gt]{as_gt()}}
}
