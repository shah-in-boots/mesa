% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-table-helpers.R
\name{model_table_helpers}
\alias{model_table_helpers}
\alias{attach_data}
\alias{model_failures}
\alias{term_table}
\alias{term_table.mdl_tbl}
\alias{term_table.fmls}
\alias{formula_matrix}
\alias{formula_matrix.mdl_tbl}
\alias{formula_matrix.fmls}
\alias{model_data}
\title{Model table helper functions}
\usage{
attach_data(x, data, name = NULL, ...)

model_failures(x, ...)

term_table(x, ...)

\method{term_table}{mdl_tbl}(x, ...)

\method{term_table}{fmls}(x, ...)

formula_matrix(x, ...)

\method{formula_matrix}{mdl_tbl}(x, ...)

\method{formula_matrix}{fmls}(x, ...)

model_data(x, name = NULL, ...)
}
\arguments{
\item{x}{A \code{mdl_tbl} object (or, for \code{term_table()} and
\code{formula_matrix()}, a \code{fmls} object)}

\item{data}{A \code{data.frame} object that has been used by models}

\item{name}{For \code{attach_data()}, the name to store the dataset under
(defaults to the expression \code{data} was passed as); for \code{model_data()},
the name of a single attached dataset to return (when \code{NULL}, the full
named list is returned)}

\item{...}{Arguments to be passed to or from other methods}
}
\value{
\code{attach_data()} returns the modified \code{mdl_tbl}; \code{model_failures()}
returns a \code{tibble} with one row per failed model and its \code{error}
message; \code{term_table()} returns a \code{tm} vector; \code{formula_matrix()}
returns a \code{tibble}; \code{model_data()} returns a named \code{list} of data frames
(or a single \code{data.frame} when \code{name} is given).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

These functions manage and interrogate a \code{mdl_tbl} object -- they are the
working verbs of the notebook of models:
\itemize{
\item \code{attach_data()}: attaches a dataset to the table for later recall
\item \code{model_failures()}: returns the models that were attempted but failed,
with their error messages
\item \code{term_table()}: the terms behind the table, as a \code{tm} vector with their
causal roles
\item \code{formula_matrix()}: the model-by-term membership matrix
\item \code{model_data()}: the datasets attached to the table
}

See \code{\link[=flatten_models]{flatten_models()}} for extracting parameter estimates.
}
\section{Attaching Data}{
When models are built, oftentimes the included matrix of data is available
within the raw model, however when handling many models, this can be
expensive in terms of memory and space. By attaching datasets independently
that persist regardless of the underlying models, and by knowing which
models used which datasets, it can be easy to back-transform information.
The dataset is stored under the name it was passed as (or an explicit
\code{name}), and should match the \code{data_id} column of the models that used it.
}

