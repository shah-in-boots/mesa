% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-columns.R
\name{add_forest}
\alias{add_forest}
\title{Add a forest column to a \verb{<mdl_gt>}}
\usage{
add_forest(x, axis = list(), width = 100, invert = FALSE)
}
\arguments{
\item{x}{A \verb{<mdl_gt>} specification (from \code{\link[=mdl_gt]{mdl_gt()}})}

\item{axis}{Options overriding the guessed x-scale, as a named list:
\code{limits} (length-2 numeric), \code{breaks} (numeric), \code{intercept} (the
reference line), \code{log} (\code{TRUE} for a log scale), and \code{title} (a string
drawn beneath the axis strip's tick labels, e.g. \code{"Hazard ratio"})}

\item{width}{Width of the drawn cells, in pixels}

\item{invert}{Show reciprocal estimates: each cell draws \code{1 / estimate}
with the interval bounds swapped and inverted, so a protective ratio
reads as risk (and vice versa). The axis mirrors with the cells, since
the shared scale is resolved from the drawn values.}
}
\value{
The modified \verb{<mdl_gt>} specification.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{add_forest()} appends a forest-plot column block to a \code{\link[=mdl_gt]{mdl_gt()}}
specification — available to any table, not just interaction tables. Its
cells \emph{read} the estimate and interval already on the specification and
compute nothing new, so the block requires \code{estimate} + \code{conf} statistics
(the bare default carries them; an \code{\link[=add_estimates]{add_estimates()}} block must keep them).

Per the grammar, the block is resolved at render: forest cells enter the
cell frame as plain numbers, \code{\link[=as_gt]{as_gt()}} resolves one shared x-scale across
the whole column (limits, intercept, breaks, log versus linear — with
\code{axis} overriding the guesses), draws each cell, and emits the bottom axis
strip as a reserved row after every row group. Adding or dropping the
block never changes any other cell.

The block's dense look (zero vertical padding, a borderless table body —
the journal convention of top rule, header rule, bottom rule, and nothing
inside) enters as \emph{defaults} to the style layer; \code{\link[=modify_style]{modify_style()}}'s
\code{padding} overrides the padding.
}
\seealso{
\code{\link[=mdl_gt]{mdl_gt()}}, \code{\link[=as_gt]{as_gt()}}, \code{\link[=add_estimates]{add_estimates()}}
}
