% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-spec.R
\name{modify_style}
\alias{modify_style}
\title{Style a \verb{<mdl_gt>} at render}
\usage{
modify_style(
  x,
  accents = NULL,
  digits = NULL,
  missing_text = NULL,
  padding = NULL
)
}
\arguments{
\item{x}{A \verb{<mdl_gt>} specification}

\item{accents}{A formula or list of formulas: \code{criterion ~ instruction}
(see Description)}

\item{digits}{Number of digits estimates are formatted to}

\item{missing_text}{Text shown in cells with nothing to display}

\item{padding}{Vertical padding scale, \code{0} (dense) upward}
}
\value{
The modified \verb{<mdl_gt>} specification.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{modify_style()} records style instructions, applied when the table is
rendered by \code{\link[=as_gt]{as_gt()}}:
\itemize{
\item \code{accents} emphasize the cells that meet a criterion. Each accent is a
two-sided formula whose \strong{left side} is a criterion on any displayed
statistic (\code{p < 0.05}, \code{estimate > 1}, \code{rate >= 10}) and whose \strong{right
side} is the instruction: \code{"bold"}, \code{"italic"}, or a text color (any
color name or hex value); several may be combined
(\code{p < 0.05 ~ c("bold", "italic")}). A criterion is evaluated once per
term-level within each row, against all of that context's statistics, and
accents every cell of the context -- so \code{p < 0.05 ~ "bold"} bolds both the
estimate and the p-value it belongs to. The recognized statistic names
are \code{estimate} (alias \code{beta}), \code{conf_low}, \code{conf_high}, \code{p} (alias
\code{p_value}), \code{n}, \code{events}, \code{rate}, and \code{rate_difference}.
\item \code{digits} sets the table-wide formatting default. A column block's own
\code{digits} still wins for its columns; p-values keep their three-decimal
rule.
\item \code{missing_text} fills every cell with nothing to show (reference levels,
estimates a model did not produce). The default is an empty cell.
\item \code{padding} scales the table's vertical row padding (as
\code{\link[gt:opt_vertical_padding]{gt::opt_vertical_padding()}} does). A table carrying a forest column
defaults to \code{0} -- the dense canvas its plot cells need to read as one --
and this overrides that default.
}

Like every verb, \code{modify_style()} merges: naming \code{digits} again after an
earlier \code{accents} call replaces only \code{digits}, with a message naming it --
the accents already recorded stand. Each of \code{accents}, \code{digits},
\code{missing_text}, and \code{padding} replaces only itself.
}
\seealso{
\code{\link[=mdl_gt]{mdl_gt()}}, \code{\link[=as_gt]{as_gt()}}
}
