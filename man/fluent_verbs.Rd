% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fluency.R
\name{fluent_verbs}
\alias{fluent_verbs}
\alias{add_strata}
\alias{add_strata.fmls}
\alias{remove_strata}
\alias{remove_strata.fmls}
\alias{add_terms}
\alias{add_terms.fmls}
\alias{remove_terms}
\alias{remove_terms.fmls}
\alias{swap_outcome}
\alias{swap_outcome.fmls}
\alias{subset_data}
\alias{subset_data.fmls}
\title{Fluent verbs for playing with formula families}
\usage{
add_strata(x, ...)

\method{add_strata}{fmls}(x, ...)

remove_strata(x, ...)

\method{remove_strata}{fmls}(x, ...)

add_terms(x, ...)

\method{add_terms}{fmls}(x, ..., role = "predictor")

remove_terms(x, ...)

\method{remove_terms}{fmls}(x, ...)

swap_outcome(x, spec)

\method{swap_outcome}{fmls}(x, spec)

subset_data(x, ...)

\method{subset_data}{fmls}(x, ...)
}
\arguments{
\item{x}{A \code{fmls} object}

\item{...}{Terms as bare names or strings; for \code{subset_data()}, one or more
logical expressions in the data's variables; for \code{remove_strata()}, if
empty, all strata are removed}

\item{role}{For \code{add_terms()}, the role the new terms should carry
(defaults to \code{"predictor"})}

\item{spec}{For \code{swap_outcome()}, either a two-sided formula \code{old ~ new} or
a bare name/string of the replacement outcome}
}
\value{
A modified \code{fmls} object
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A family of formulas should feel like something to play with: pick terms up
off a dataset, snap them together, swap the pieces, and watch the family of
models unfold. These pipeable verbs each take a \code{fmls} object and return a
modified one, so a modeling deck can be grown interactively:
\itemize{
\item \code{add_strata()} / \code{remove_strata()}: mark terms as stratifying variables
(\code{\link[=fit]{fit()}} will fit one model per stratum level)
\item \code{add_terms()} / \code{remove_terms()}: add or drop covariates from every
formula in the family
\item \code{swap_outcome()}: exchange one outcome for another, keeping the rest of
the family intact
\item \code{subset_data()}: record data-filtering instructions (e.g. \code{sex == "F"})
that \code{\link[=fit]{fit()}} will apply, fitting the family once per subset
}

Terms may be given as bare names or strings. For \code{swap_outcome()}, a
two-sided formula \code{old ~ new} swaps a specific outcome; a bare name is
allowed when the family has a single outcome.
}
\examples{
f <- fmls(mpg ~ .x(wt) + hp)
f |>
  add_strata(am) |>
  add_terms(cyl) |>
  subset_data(disp > 100)

}
