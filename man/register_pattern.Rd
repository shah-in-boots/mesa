% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patterns.R
\name{register_pattern}
\alias{register_pattern}
\title{Register a formula expansion pattern}
\usage{
register_pattern(name, fn)
}
\arguments{
\item{name}{A single string naming the pattern}

\item{fn}{A function accepting a \code{tm} vector and returning a \code{tbl_df} that
follows the pattern contract}
}
\value{
The pattern name, invisibly
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Patterns are the rules by which a set of terms expands into a family of
formulas. Each pattern is a function that takes a \code{tm} vector and returns a
\code{tbl_df} precursor table (see \code{\link[=apply_pattern]{apply_pattern()}} for the contract). The
built-in patterns (fundamental, direct, sequential, parallel) are registered
this way; user-defined patterns can join them and become available to
\code{\link[=fmls]{fmls()}} by name.
}
\examples{
# A pattern that ignores covariates entirely
unadjusted <- function(x) {
  apply_fundamental_pattern(x)
}
register_pattern("unadjusted", unadjusted)
"unadjusted" \%in\% formula_patterns()

}
