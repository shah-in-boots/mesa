% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_plan}
\alias{fit_plan}
\title{Draw up the fitting plan for a family of formulas}
\usage{
fit_plan(object, data = NULL, ...)
}
\arguments{
\item{object}{A \code{fmls} object}

\item{data}{An optional \code{data.frame}; when supplied, stratum levels are
enumerated from the data, otherwise they are left unresolved (\code{NA})}

\item{...}{Arguments to be passed to or from other methods}
}
\value{
A \code{tbl_df} with one row per model to be fit and the columns
\code{formula_index}, \code{formula_call}, \code{formula} (as a list), \code{strata_variable},
\code{strata_level} (as a list, since levels keep their native type),
\code{subset}, and \code{subset_expr}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Every call to \code{\link[=fit]{fit()}} first builds a plan: one row per model that will be
fit, crossing each formula in the family with each stratum level of its
\code{.s()} terms and each subset instruction from \code{\link[=subset_data]{subset_data()}}. Exposing the
plan lets the shape of an analysis be inspected — and played with — before
any model is run.
}
