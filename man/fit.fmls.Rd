% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit.fmls}
\alias{fit.fmls}
\title{Fit the family of models a \code{fmls} object describes}
\usage{
\method{fit}{fmls}(object, .fn, ..., data, raw = TRUE)
}
\arguments{
\item{object}{A \code{fmls} object}

\item{.fn}{The modeling approach, given as any of:
\itemize{
\item a fitting function, e.g. \code{lm} or \code{lme4::lmer}
\item the name of a fitting function, e.g. \code{"glm"}
\item a \code{{parsnip}} model specification, e.g.
\code{parsnip::logistic_reg() |> parsnip::set_engine("glm")}, which lets any
engine \code{{parsnip}} knows about serve as the modeling approach
}}

\item{...}{Additional arguments passed to the fitting function (e.g.
\code{family = "binomial"})}

\item{data}{A \code{data.frame} containing the modeling variables}

\item{raw}{A \code{logical}. When \code{TRUE} (default), returns the list of fitted
model objects as the fitting function made them (for \code{{parsnip}}
specifications, the underlying engine fit). When \code{FALSE}, returns a
\code{mdl} vector that carries the causal context forward into
\code{\link[=model_table]{model_table()}}.}
}
\value{
A \code{list} of models (when \code{raw = TRUE}) or a \code{mdl} vector
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Fitting happens in two steps: a \emph{plan} is drawn up, then executed. The plan
crosses every formula in the family with every stratum level (from \code{.s()}
terms) and every subset instruction (from \code{\link[=subset_data]{subset_data()}}), so one \code{fmls}
object can quietly describe dozens of models. The plan itself can be
inspected before anything is fit through \code{\link[=fit_plan]{fit_plan()}} — this is the "play"
step, where the shape of the analysis is visible before it runs.

Failures are soft: if one model in a batch cannot be fit, it is recorded
with its error message (\code{fit_status} becomes \code{FALSE} in a downstream
\code{\link[=model_table]{model_table()}}) rather than sinking the rest of the fleet.
}
