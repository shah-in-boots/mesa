# Can `mdl_tbl` be reconstructed based on invariants?

Can `mdl_tbl` be reconstructed based on invariants?

## Usage

``` r
model_table_reconstructable(x, to)
```

## Arguments

- x:

  data frame that will have invariants checked

- to:

  the tibble subclass of `mdl_tbl` that would be reconstructed
